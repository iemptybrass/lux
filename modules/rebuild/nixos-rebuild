#!/usr/bin/env bash
if [ -e "$3" ]; then
  echo "Directory exists"
  dir="$3"
else
  echo "Directory does not exist"
  dir="$(pwd)"
fi

echo "$dir"
/run/current-system/sw/bin/nixos-rebuild "$@"

echo "finished"